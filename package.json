{
  "name": "widgets",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url /",
    "docker:build": "docker build . -t raduwen/widgets"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "paho-mqtt": "1.1.0"
  }
}
